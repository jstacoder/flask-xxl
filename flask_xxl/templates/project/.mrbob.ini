[questions]
project.name.question = What will you name this webapp
project.name.required = True
project.name.help = You need a name to start a good project

project.use_database.question = Will this webapp need database support
project.use_database.required = True
project.use_database.default = False
project.use_database.post_ask_question = mrbob.hooks:val

project.db-username.question

project.db-password.question

project.db-host.question

project.db-port.question

project.db-name.question

project.db-driver.question

project.add_migrations.question
