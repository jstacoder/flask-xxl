[defaults]
blueprint.template_dir = 'templates'
blueprint.main_template = index.html

[questions]
blueprint.name.question = what will you name this blueprint
blueprint.name.required = True

blueprint.url_prefix.question = what url prefix should the blueprint be avialble at
blueprint.url_prefix.required = True

blueprint.template_dir.question = where will you put the templates

blueprint.main_template.question = what template should the main view use

blueprint.model.question = what should the first model be called
